# ===========================================
# smartfarm-web 환경 변수 예시
# 실제 값은 .env.local 에 넣고 Git 에 커밋하지 마세요.
# ===========================================

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# 서버 전용 (API Routes 등). 클라이언트에 노출 금지
SUPABASE_SERVICE_ROLE_KEY=

# --- HiveMQ Cloud (MQTT, 서버 전용) ---
# 8884 WebSocket: wss://<cluster-id>.s1.eu.hivemq.cloud:8884/mqtt
# 8883 TCP: mqtts://<cluster-id>.s1.eu.hivemq.cloud:8883
MQTT_BROKER_URL=
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=

# --- A방식: 브라우저에서 MQTT 구독 (Vercel만 사용 시) ---
# wss:// 형식만 사용. 설정 시 대시보드가 MQTT에 직접 연결해 실시간 표시.
# 주의: 브라우저에 노출되므로 읽기 전용용 계정 권장.
NEXT_PUBLIC_MQTT_BROKER_URL=
NEXT_PUBLIC_MQTT_USERNAME=
NEXT_PUBLIC_MQTT_PASSWORD=

# --- Vercel (배포 시 자동 주입 가능) ---
# VERCEL_URL=
# VERCEL_ENV=
